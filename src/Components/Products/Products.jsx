import { react, use } from "react";


const Products = ({prodData}) => {

   const data = use(prodData)
   console.log(data)

    return (
        <div>
            
        </div>
    );
};

export default Products;